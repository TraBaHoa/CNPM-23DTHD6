@{
    ViewData["Title"] = "Đăng nhập hệ thống";
}

<div class="container mt-5" style="max-width: 450px;">
    <div class="card shadow-lg border-0 rounded-4 overflow-hidden">
        <div class="card-header bg-primary text-white text-center py-4">
            <h4 class="mb-0 fw-bold">HỆ THỐNG ĐÁNH GIÁ THUỐC</h4>
            <small class="text-white-50">Dành cho bệnh nhân điều trị nội viện</small>
        </div>
        
        <div class="card-body p-4">
            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger text-center rounded-3 shadow-sm border-0 mb-4 animate__animated animate__shakeX">
                    <i class="bi bi-exclamation-triangle-fill"></i> @ViewBag.Error
                </div>
            }
            
            <form asp-action="Login" method="post" id="loginForm" class="needs-validation" novalidate>
                @Html.AntiForgeryToken()
    
                <div class="form-group mb-3">
                    <label class="form-label fw-bold text-muted small text-uppercase">Mã số bệnh nhân</label>
                    <input type="text"
                           name="maBenhNhan"
                           class="form-control form-control-lg text-center shadow-sm"
                           placeholder="Ví dụ: BN001"
                           required
                           autofocus
                           autocomplete="off" />
                </div>

                <div class="form-group mb-4">
                    <label class="form-label fw-bold text-muted small text-uppercase">Mật khẩu</label>
                    <input type="password"
                           name="matKhau"
                           class="form-control form-control-lg text-center shadow-sm"
                           placeholder="••••••"
                           required />
                </div>
    
                <button type="submit" id="btnSubmit" class="btn btn-success btn-lg w-100 shadow-sm fw-bold transition-all">
                    ĐĂNG NHẬP ➔
                </button>
            </form>
        </div>
        
        <div class="card-footer bg-light text-center py-3 border-0">
            <small class="text-muted">Cần hỗ trợ? Vui lòng liên hệ Dược sĩ lâm sàng.</small>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Hiệu ứng loading chuyên nghiệp
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            const btn = document.getElementById('btnSubmit');
            if (this.checkValidity()) {
                btn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span> ĐANG XỬ LÝ...';
                btn.disabled = true;
            }
        });
    </script>
}